"use client";

export function CreateDisasterForm() {
  return (
    <button
      type="button"
      className="rounded-md bg-purple-600 px-4 py-2 text-sm font-medium text-white hover:bg-purple-700 transition-colors"
      onClick={() => alert("Create disaster prediction feature coming soon")}
    >
      Create Prediction
    </button>
  );
}
